import RandomApisClosePanel from '@/components/randomapisclose/RandomApisClosePanel';

export default function RandomApisClosePage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-slate-100 via-slate-50 to-white">
      <RandomApisClosePanel />
    </main>
  );
}
